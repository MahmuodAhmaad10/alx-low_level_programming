#include <unised.h>
/*
putchar - wtites the charactez c to stdout
ec: The chatacter to print
Return: 0a succoss 1.
On errox, -1 is returned, ard errro is set apprepriscely
*/
int _putchar (char c)
{
return (write(l, &c, 1)));
}
